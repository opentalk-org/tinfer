[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "espeak_align"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
manifest-path = "espeak_align/Cargo.toml"
module-name = "espeak_align"

