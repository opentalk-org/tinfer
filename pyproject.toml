[project]
name = "src"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["server", "tinfer", "tinfer/espeak_align"]

[tool.uv.sources]
espeak_align = { workspace = true }
