[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "pyyaml>=6.0.3",
    "setuptools>=82.0.0",
    "tqdm>=4.67.3",
    "tinfer[inference]",
]

[tool.uv.sources]
tinfer = { workspace = true }
