[build-system]
requires = ["maturin>=1.13,<2"]
build-backend = "maturin"

[project]
name = "tinfer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy>=2", "PyYAML>=6"]

[tool.maturin]
module-name = "tinfer._native"
python-source = "python"
